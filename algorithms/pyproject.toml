[project]
name = "algorithms"
version = "0.1.1"
description = "Simple algorithms in Python"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "iniconfig>=2.3.0",
    "pytest==9.0.2",
]

[tool.uv.workspace]
members = [
    "floodfill",
]
